version: 2

sources:
  - name: jaffle_shop
    database: Bakery_DB
    schema: jaffle_shop
    tables:
      - name: CUSTOMERS
        # columns:
        #   - name: id
        #     tests:
        #       - unique
        #       - not_null
      - name: ORDERS
        config:
          loaded_at_field: _etl_loaded_at
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}
        # columns:
        #   - name: id
        #     tests:
        #       - not_null
        #       - unique
        #   - name: status
        #     tests:
        #       - accepted_values
        #           agruments:

